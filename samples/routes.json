{

    "index": {
        "GET": {
            "function": "index",
            "authentication": false,
            "setup_transaction": false,
            "parameters": []
        }
    },

    "send_event": {
        "POST": {
            "function": "send_event",
            "authentication": true,
            "setup_transaction": true,
            "parameters": [
                { "name": "queue", "kind": "enum", "required": true, "max_length": 0, "options": [ "opt1", "opt2" ] },
                { "name": "key", "kind": "string", "required": true, "max_length": 0, "options": [] },
                { "name": "event", "kind": "string", "required": true, "max_length": 0, "options": [] },
                { "name": "payload", "kind": "map", "required": true, "max_length": 0, "options": [] }
            ]
        }
    }

}